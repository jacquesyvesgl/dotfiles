# !/bin/sh

export PATH=/home/jacquesyves/.local/bin:$PATH
export PATH=/home/jacquesyves/.local/share/texlive/bin/x86_64-linux:$PATH
export PATH=/home/jacquesyves/miniconda3/bin:$PATH

if [ -n "$DESKTOP_SESSION" ];then
    # eval $(gnome-keyring-daemon --start &> /dev/null) 
    eval $(gnome-keyring-daemon --start 2> /dev/null) 
    # eval $(gnome-keyring-daemon --start)
    export SSH_AUTH_SOCK
fi
